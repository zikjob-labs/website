import { Link } from 'react-router-dom';

function WalletRegisterPage() {
  return (
    <div>
      <p>Wallet Register Page</p>
      <p>
        <Link to={`/`}>Back</Link>
      </p>
    </div>
  );
}

export default WalletRegisterPage;
